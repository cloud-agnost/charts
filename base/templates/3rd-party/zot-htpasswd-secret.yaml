apiVersion: v1
kind: Secret
metadata:
  name: zot-htpasswd
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  htpasswd: | 
    {{ htpasswd "admin" (derivePassword 1 "maximum" .Release.Name .Chart.Name (lookup "v1" "Service" "default" "kubernetes").metadata.creationTimestamp | b64enc | trimSuffix "=") }}
